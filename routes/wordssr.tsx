import { PageProps } from "$fresh/server.ts";


const Page = (props: PageProps) => {
    return (
        <div class="fondo">
            <strong>Not implemented</strong>
        </div>
    )
}

export default Page;
